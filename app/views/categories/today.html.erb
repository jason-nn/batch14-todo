<div>
  <div class="header">Due today</div>
  <div class="categories-container">
    <% @categories.each do |category| %>
      <% if category.tasks.where(deadline: Date.today).length > 0 %>
        <%= render 'today', category: category %>
      <% end %>
    <% end %>
    <div class="new-category-button-container">
      <%= link_to new_category_path do %>
        <div class="button-blue new-category-button-container-button">􀅼</div>
      <% end %>
    </div>
  </div>
</div>
